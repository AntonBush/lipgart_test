#include "climate_system.h"

#include <stdlib.h>

struct ClimateSystem * switchClimateSystem(
	  struct ClimateSystem * system
    , int delta_t                   )
{
	if (system == NULL)
	{
		return NULL;
	}



	return system;
}
